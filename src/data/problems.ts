import type { Problem } from "../types";

export const PROBLEMS: Problem[] = [
  {
    id: 1,
    slug: "two-sum",
    title: "Two Sum",
    difficulty: "Easy",
    language: "Python",
    topic: "Arrays & Hashing",
    tags: ["Array", "Hash Table"],
    xpReward: 50,
    acceptance: "49.8%",
    description: `Given an array of integers <code>nums</code> and an integer <code>target</code>, return <em>indices of the two numbers such that they add up to <code>target</code></em>.<br/><br/>You may assume that each input would have <strong>exactly one solution</strong>, and you may not use the same element twice.<br/><br/>You can return the answer in any order.`,
    examples: [
      {
        input: "nums = [2,7,11,15], target = 9",
        output: "[0,1]",
        explanation: "Because nums[0] + nums[1] == 9, we return [0, 1].",
      },
      {
        input: "nums = [3,2,4], target = 6",
        output: "[1,2]",
        explanation: "Because nums[1] + nums[2] == 6, we return [1, 2].",
      },
      {
        input: "nums = [3,3], target = 6",
        output: "[0,1]",
        explanation: "Because nums[0] + nums[1] == 6, we return [0, 1].",
      },
    ],
    constraints: [
      "2 <= nums.length <= 10^4",
      "-10^9 <= nums[i] <= 10^9",
      "-10^9 <= target <= 10^9",
      "Only one valid answer exists.",
    ],
    hints: [
      "A really brute force way would be to search for all possible pairs of numbers but that would be slow. Can you do it in O(n) time?",
      "Can we use extra space? What if we stored each number we have seen so far in a hash table mapping value to index?",
      "As you iterate through the array, check if target - nums[i] already exists in the map.",
    ],
    starterCodes: {
      Python: `class Solution:\n    def twoSum(self, nums: list[int], target: int) -> list[int]:\n        # Write your solution here\n        seen = {}\n        for i, num in enumerate(nums):\n            diff = target - num\n            if diff in seen:\n                return [seen[diff], i]\n            seen[num] = i\n        return []`,
      "C++": `class Solution {\npublic:\n    vector<int> twoSum(vector<int>& nums, int target) {\n        unordered_map<int, int> seen;\n        for (int i = 0; i < nums.size(); i++) {\n            int diff = target - nums[i];\n            if (seen.find(diff) != seen.end()) {\n                return {seen[diff], i};\n            }\n            seen[nums[i]] = i;\n        }\n        return {};\n    }\n};`,
      Java: `class Solution {\n    public int[] twoSum(int[] nums, int target) {\n        Map<Integer, Integer> map = new HashMap<>();\n        for (int i = 0; i < nums.length; i++) {\n            int complement = target - nums[i];\n            if (map.containsKey(complement)) {\n                return new int[] { map.get(complement), i };\n            }\n            map.put(nums[i], i);\n        }\n        return new int[] {};\n    }\n}`,
      C: `/**\n * Note: The returned array must be malloced, assume caller calls free().\n */\nint* twoSum(int* nums, int numsSize, int target, int* returnSize) {\n    *returnSize = 2;\n    int* res = (int*)malloc(2 * sizeof(int));\n    for (int i = 0; i < numsSize; i++) {\n        for (int j = i + 1; j < numsSize; j++) {\n            if (nums[i] + nums[j] == target) {\n                res[0] = i;\n                res[1] = j;\n                return res;\n            }\n        }\n    }\n    return res;\n}`,
    },
    testCases: [
      {
        id: "tc-1",
        input: "nums = [2,7,11,15], target = 9",
        expectedOutput: "[0,1]",
        actualOutput: "[0,1]",
        passed: true,
        runtimeMs: 4,
        memoryMb: 14.8,
      },
      {
        id: "tc-2",
        input: "nums = [3,2,4], target = 6",
        expectedOutput: "[1,2]",
        actualOutput: "[1,2]",
        passed: true,
        runtimeMs: 3,
        memoryMb: 14.7,
      },
      {
        id: "tc-3",
        input: "nums = [3,3], target = 6",
        expectedOutput: "[0,1]",
        actualOutput: "[0,1]",
        passed: true,
        runtimeMs: 4,
        memoryMb: 14.6,
      },
      {
        id: "tc-4",
        input: "nums = [-1,-2,-3,-4,-5], target = -8",
        expectedOutput: "[2,4]",
        actualOutput: "[2,4]",
        passed: true,
        runtimeMs: 5,
        memoryMb: 14.9,
      },
    ],
    solved: true,
  },
  {
    id: 2,
    slug: "longest-palindromic-substring",
    title: "Longest Palindromic Substring",
    difficulty: "Medium",
    language: "Python",
    topic: "Dynamic Programming",
    tags: ["String", "Dynamic Programming", "Two Pointers"],
    xpReward: 100,
    acceptance: "33.4%",
    description: `Given a string <code>s</code>, return <em>the longest palindromic substring</em> in <code>s</code>.<br/><br/>A string is palindromic if it reads the same forward and backward.`,
    examples: [
      {
        input: 's = "babad"',
        output: '"bab"',
        explanation: '"aba" is also a valid answer.',
      },
      {
        input: 's = "cbbd"',
        output: '"bb"',
        explanation: 'The substring "bb" is the longest palindrome.',
      },
    ],
    constraints: [
      "1 <= s.length <= 1000",
      "s consist of only digits and English letters.",
    ],
    hints: [
      "How can we reuse a previously computed palindrome to check a larger substring?",
      "If 'aba' is a palindrome, is 'xabax' also a palindrome? Yes, because s[0] == s[4] and 'aba' is a palindrome.",
      "Notice that a palindrome mirrors around its center. A string of length N has 2N - 1 centers (each character and between characters).",
    ],
    starterCodes: {
      Python: `class Solution:\n    def longestPalindrome(self, s: str) -> str:\n        if not s:\n            return ""\n        start, max_len = 0, 1\n        def expand(left: int, right: int):\n            nonlocal start, max_len\n            while left >= 0 and right < len(s) and s[left] == s[right]:\n                if right - left + 1 > max_len:\n                    start = left\n                    max_len = right - left + 1\n                left -= 1\n                right += 1\n        for i in range(len(s)):\n            expand(i, i)\n            expand(i, i + 1)\n        return s[start : start + max_len]`,
      "C++": `class Solution {\npublic:\n    string longestPalindrome(string s) {\n        if (s.empty()) return "";\n        int start = 0, maxLen = 1;\n        auto expand = [&](int left, int right) {\n            while (left >= 0 && right < s.length() && s[left] == s[right]) {\n                if (right - left + 1 > maxLen) {\n                    start = left;\n                    maxLen = right - left + 1;\n                }\n                left--;\n                right++;\n            }\n        };\n        for (int i = 0; i < s.length(); i++) {\n            expand(i, i);\n            expand(i, i + 1);\n        }\n        return s.substr(start, maxLen);\n    }\n};`,
      Java: `class Solution {\n    public String longestPalindrome(String s) {\n        if (s == null || s.length() < 1) return "";\n        int start = 0, end = 0;\n        for (int i = 0; i < s.length(); i++) {\n            int len1 = expandAroundCenter(s, i, i);\n            int len2 = expandAroundCenter(s, i, i + 1);\n            int len = Math.max(len1, len2);\n            if (len > end - start) {\n                start = i - (len - 1) / 2;\n                end = i + len / 2;\n            }\n        }\n        return s.substring(start, end + 1);\n    }\n    private int expandAroundCenter(String s, int left, int right) {\n        while (left >= 0 && right < s.length() && s.charAt(left) == s.charAt(right)) {\n            left--;\n            right++;\n        }\n        return right - left - 1;\n    }\n}`,
      C: `char* longestPalindrome(char* s) {\n    int n = strlen(s);\n    if (n <= 1) return s;\n    int start = 0, maxLen = 1;\n    for (int i = 0; i < n; i++) {\n        int l = i, r = i;\n        while (l >= 0 && r < n && s[l] == s[r]) {\n            if (r - l + 1 > maxLen) { start = l; maxLen = r - l + 1; }\n            l--; r++;\n        }\n        l = i; r = i + 1;\n        while (l >= 0 && r < n && s[l] == s[r]) {\n            if (r - l + 1 > maxLen) { start = l; maxLen = r - l + 1; }\n            l--; r++;\n        }\n    }\n    char* res = (char*)malloc((maxLen + 1) * sizeof(char));\n    strncpy(res, s + start, maxLen);\n    res[maxLen] = '\\0';\n    return res;\n}`,
    },
    testCases: [
      {
        id: "tc-1",
        input: 's = "babad"',
        expectedOutput: '"bab"',
        actualOutput: '"bab"',
        passed: true,
        runtimeMs: 14,
        memoryMb: 15.2,
      },
      {
        id: "tc-2",
        input: 's = "cbbd"',
        expectedOutput: '"bb"',
        actualOutput: '"bb"',
        passed: true,
        runtimeMs: 11,
        memoryMb: 15.1,
      },
      {
        id: "tc-3",
        input: 's = "a"',
        expectedOutput: '"a"',
        actualOutput: '"a"',
        passed: true,
        runtimeMs: 6,
        memoryMb: 14.9,
      },
      {
        id: "tc-4",
        input: 's = "racecar"',
        expectedOutput: '"racecar"',
        actualOutput: '"racecar"',
        passed: true,
        runtimeMs: 12,
        memoryMb: 15.3,
      },
    ],
    solved: false,
  },
  {
    id: 3,
    slug: "merge-k-sorted-lists",
    title: "Merge k Sorted Lists",
    difficulty: "Hard",
    language: "C++",
    topic: "Linked Lists & Heaps",
    tags: ["Linked List", "Divide and Conquer", "Heap (Priority Queue)"],
    xpReward: 200,
    acceptance: "51.2%",
    description: `You are given an array of <code>k</code> linked-lists <code>lists</code>, each linked-list is sorted in ascending order.<br/><br/><em>Merge all the linked-lists into one sorted linked-list and return it.</em>`,
    examples: [
      {
        input: "lists = [[1,4,5],[1,3,4],[2,6]]",
        output: "[1,1,2,3,4,4,5,6]",
        explanation: "The linked-lists are:\n[\n  1->4->5,\n  1->3->4,\n  2->6\n]\nmerging them into one sorted list: 1->1->2->3->4->4->5->6",
      },
      {
        input: "lists = []",
        output: "[]",
      },
      {
        input: "lists = [[]]",
        output: "[]",
      },
    ],
    constraints: [
      "k == lists.length",
      "0 <= k <= 10^4",
      "0 <= lists[i].length <= 500",
      "-10^4 <= lists[i][j] <= 10^4",
      "lists[i] is sorted in ascending order.",
    ],
    hints: [
      "Can you merge lists two by two like Merge Sort divide and conquer?",
      "Alternatively, can you use a min-heap to keep track of the smallest current element among the k lists?",
    ],
    starterCodes: {
      "C++": `/**\n * Definition for singly-linked list.\n * struct ListNode {\n *     int val;\n *     ListNode *next;\n *     ListNode() : val(0), next(nullptr) {}\n *     ListNode(int x) : val(x), next(nullptr) {}\n *     ListNode(int x, ListNode *next) : val(x), next(next) {}\n * };\n */\nclass Solution {\npublic:\n    ListNode* mergeKLists(vector<ListNode*>& lists) {\n        auto cmp = [](ListNode* a, ListNode* b) { return a->val > b->val; };\n        priority_queue<ListNode*, vector<ListNode*>, decltype(cmp)> pq(cmp);\n        for (auto l : lists) if (l) pq.push(l);\n        ListNode dummy(0);\n        ListNode* tail = &dummy;\n        while (!pq.empty()) {\n            auto curr = pq.top();\n            pq.pop();\n            tail->next = curr;\n            tail = tail->next;\n            if (curr->next) pq.push(curr->next);\n        }\n        return dummy.next;\n    }\n};`,
      Python: `class Solution:\n    def mergeKLists(self, lists: list[Optional[ListNode]]) -> Optional[ListNode]:\n        import heapq\n        heap = []\n        for i, l in enumerate(lists):\n            if l:\n                heapq.heappush(heap, (l.val, i, l))\n        dummy = ListNode(0)\n        curr = dummy\n        while heap:\n            val, i, node = heapq.heappop(heap)\n            curr.next = node\n            curr = curr.next\n            if node.next:\n                heapq.heappush(heap, (node.next.val, i, node.next))\n        return dummy.next`,
      Java: `class Solution {\n    public ListNode mergeKLists(ListNode[] lists) {\n        if (lists == null || lists.length == 0) return null;\n        PriorityQueue<ListNode> pq = new PriorityQueue<>((a, b) -> a.val - b.val);\n        for (ListNode node : lists) if (node != null) pq.add(node);\n        ListNode dummy = new ListNode(0);\n        ListNode tail = dummy;\n        while (!pq.isEmpty()) {\n            ListNode node = pq.poll();\n            tail.next = node;\n            tail = tail.next;\n            if (node.next != null) pq.add(node.next);\n        }\n        return dummy.next;\n    }\n}`,
      C: `struct ListNode* mergeKLists(struct ListNode** lists, int listsSize) {\n    if (listsSize == 0) return NULL;\n    // Divide and conquer pair merges\n    int interval = 1;\n    while (interval < listsSize) {\n        for (int i = 0; i < listsSize - interval; i += interval * 2) {\n            lists[i] = mergeTwo(lists[i], lists[i + interval]);\n        }\n        interval *= 2;\n    }\n    return lists[0];\n}`,
    },
    testCases: [
      {
        id: "tc-1",
        input: "lists = [[1,4,5],[1,3,4],[2,6]]",
        expectedOutput: "[1,1,2,3,4,4,5,6]",
        actualOutput: "[1,1,2,3,4,4,5,6]",
        passed: true,
        runtimeMs: 18,
        memoryMb: 16.4,
      },
      {
        id: "tc-2",
        input: "lists = []",
        expectedOutput: "[]",
        actualOutput: "[]",
        passed: true,
        runtimeMs: 4,
        memoryMb: 14.1,
      },
      {
        id: "tc-3",
        input: "lists = [[]]",
        expectedOutput: "[]",
        actualOutput: "[]",
        passed: true,
        runtimeMs: 5,
        memoryMb: 14.2,
      },
    ],
    solved: false,
  },
  {
    id: 4,
    slug: "reverse-linked-list",
    title: "Reverse Linked List",
    difficulty: "Easy",
    language: "C",
    topic: "Linked Lists",
    tags: ["Linked List", "Recursion", "Pointers"],
    xpReward: 50,
    acceptance: "74.1%",
    description: `Given the <code>head</code> of a singly linked list, reverse the list, and return <em>the reversed list</em>.`,
    examples: [
      {
        input: "head = [1,2,3,4,5]",
        output: "[5,4,3,2,1]",
      },
      {
        input: "head = [1,2]",
        output: "[2,1]",
      },
      {
        input: "head = []",
        output: "[]",
      },
    ],
    constraints: [
      "The number of nodes in the list is the range [0, 5000].",
      "-5000 <= Node.val <= 5000",
    ],
    hints: [
      "A linked list can be reversed either iteratively or recursively. Could you implement both?",
      "While traversing the list, keep track of the previous node, current node, and next node.",
    ],
    starterCodes: {
      C: `/**\n * Definition for singly-linked list.\n * struct ListNode {\n *     int val;\n *     struct ListNode *next;\n * };\n */\nstruct ListNode* reverseList(struct ListNode* head) {\n    struct ListNode* prev = NULL;\n    struct ListNode* curr = head;\n    while (curr != NULL) {\n        struct ListNode* nextTemp = curr->next;\n        curr->next = prev;\n        prev = curr;\n        curr = nextTemp;\n    }\n    return prev;\n}`,
      "C++": `class Solution {\npublic:\n    ListNode* reverseList(ListNode* head) {\n        ListNode* prev = nullptr;\n        ListNode* curr = head;\n        while (curr) {\n            ListNode* nextNode = curr->next;\n            curr->next = prev;\n            prev = curr;\n            curr = nextNode;\n        }\n        return prev;\n    }\n};`,
      Java: `class Solution {\n    public ListNode reverseList(ListNode head) {\n        ListNode prev = null;\n        ListNode curr = head;\n        while (curr != null) {\n            ListNode nextTemp = curr.next;\n            curr.next = prev;\n            prev = curr;\n            curr = nextTemp;\n        }\n        return prev;\n    }\n}`,
      Python: `class Solution:\n    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:\n        prev = None\n        curr = head\n        while curr:\n            next_node = curr.next\n            curr.next = prev\n            prev = curr\n            curr = next_node\n        return prev`,
    },
    testCases: [
      {
        id: "tc-1",
        input: "head = [1,2,3,4,5]",
        expectedOutput: "[5,4,3,2,1]",
        actualOutput: "[5,4,3,2,1]",
        passed: true,
        runtimeMs: 2,
        memoryMb: 11.2,
      },
      {
        id: "tc-2",
        input: "head = [1,2]",
        expectedOutput: "[2,1]",
        actualOutput: "[2,1]",
        passed: true,
        runtimeMs: 2,
        memoryMb: 11.1,
      },
      {
        id: "tc-3",
        input: "head = []",
        expectedOutput: "[]",
        actualOutput: "[]",
        passed: true,
        runtimeMs: 1,
        memoryMb: 11.0,
      },
    ],
    solved: true,
  },
  {
    id: 5,
    slug: "binary-tree-maximum-path-sum",
    title: "Binary Tree Maximum Path Sum",
    difficulty: "Hard",
    language: "Java",
    topic: "Trees & Graphs",
    tags: ["Dynamic Programming", "Tree", "Depth-First Search", "Binary Tree"],
    xpReward: 200,
    acceptance: "39.6%",
    description: `A <strong>path</strong> in a binary tree is a sequence of nodes where each pair of adjacent nodes in the sequence has an edge connecting them. A node can only appear in the sequence <strong>at most once</strong>. Note that the path does not need to pass through the root.<br/><br/>The <strong>path sum</strong> of a path is the sum of the node's values in the path.<br/><br/>Given the <code>root</code> of a binary tree, return <em>the maximum <strong>path sum</strong> of any <strong>non-empty</strong> path</em>.`,
    examples: [
      {
        input: "root = [1,2,3]",
        output: "6",
        explanation: "The optimal path is 2 -> 1 -> 3 with a path sum of 2 + 1 + 3 = 6.",
      },
      {
        input: "root = [-10,9,20,null,null,15,7]",
        output: "42",
        explanation: "The optimal path is 15 -> 20 -> 7 with a path sum of 15 + 20 + 7 = 42.",
      },
    ],
    constraints: [
      "The number of nodes in the tree is in the range [1, 3 * 10^4].",
      "-1000 <= Node.val <= 1000",
    ],
    hints: [
      "Think post-order traversal (DFS). For each node, what is the maximum path sum going down its left subtree vs right subtree?",
      "A path through a node can combine both left and right contributions, but returning up to the parent can only pick the single best branch.",
    ],
    starterCodes: {
      Java: `/**\n * Definition for a binary tree node.\n * public class TreeNode {\n *     int val;\n *     TreeNode left;\n *     TreeNode right;\n *     TreeNode() {}\n *     TreeNode(int val) { this.val = val; }\n *     TreeNode(int val, TreeNode left, TreeNode right) {\n *         this.val = val;\n *         this.left = left;\n *         this.right = right;\n *     }\n * }\n */\nclass Solution {\n    private int maxSum = Integer.MIN_VALUE;\n    public int maxPathSum(TreeNode root) {\n        maxGain(root);\n        return maxSum;\n    }\n    private int maxGain(TreeNode node) {\n        if (node == null) return 0;\n        int leftGain = Math.max(maxGain(node.left), 0);\n        int rightGain = Math.max(maxGain(node.right), 0);\n        int priceNewPath = node.val + leftGain + rightGain;\n        maxSum = Math.max(maxSum, priceNewPath);\n        return node.val + Math.max(leftGain, rightGain);\n    }\n}`,
      "C++": `class Solution {\n    int maxSum = INT_MIN;\n    int maxGain(TreeNode* node) {\n        if (!node) return 0;\n        int left = max(maxGain(node->left), 0);\n        int right = max(maxGain(node->right), 0);\n        maxSum = max(maxSum, node->val + left + right);\n        return node->val + max(left, right);\n    }\npublic:\n    int maxPathSum(TreeNode* root) {\n        maxGain(root);\n        return maxSum;\n    }\n};`,
      Python: `class Solution:\n    def maxPathSum(self, root: Optional[TreeNode]) -> int:\n        max_sum = float('-inf')\n        def max_gain(node):\n            nonlocal max_sum\n            if not node:\n                return 0\n            left_gain = max(max_gain(node.left), 0)\n            right_gain = max(max_gain(node.right), 0)\n            max_sum = max(max_sum, node.val + left_gain + right_gain)\n            return node.val + max(left_gain, right_gain)\n        max_gain(root)\n        return int(max_sum)`,
      C: `int max(int a, int b) { return a > b ? a : b; }\nint maxGain(struct TreeNode* node, int* maxSum) {\n    if (!node) return 0;\n    int left = max(maxGain(node->left, maxSum), 0);\n    int right = max(maxGain(node->right, maxSum), 0);\n    int pathSum = node->val + left + right;\n    if (pathSum > *maxSum) *maxSum = pathSum;\n    return node->val + max(left, right);\n}\nint maxPathSum(struct TreeNode* root) {\n    int maxSum = -2147483648;\n    maxGain(root, &maxSum);\n    return maxSum;\n}`,
    },
    testCases: [
      {
        id: "tc-1",
        input: "root = [1,2,3]",
        expectedOutput: "6",
        actualOutput: "6",
        passed: true,
        runtimeMs: 1,
        memoryMb: 42.4,
      },
      {
        id: "tc-2",
        input: "root = [-10,9,20,null,null,15,7]",
        expectedOutput: "42",
        actualOutput: "42",
        passed: true,
        runtimeMs: 1,
        memoryMb: 43.1,
      },
      {
        id: "tc-3",
        input: "root = [-3]",
        expectedOutput: "-3",
        actualOutput: "-3",
        passed: true,
        runtimeMs: 0,
        memoryMb: 41.9,
      },
    ],
    solved: false,
  },
  {
    id: 6,
    slug: "valid-anagram",
    title: "Valid Anagram",
    difficulty: "Easy",
    language: "Python",
    topic: "Arrays & Hashing",
    tags: ["Hash Table", "String", "Sorting"],
    xpReward: 50,
    acceptance: "63.2%",
    description: `Given two strings <code>s</code> and <code>t</code>, return <code>true</code> if <code>t</code> is an anagram of <code>s</code>, and <code>false</code> otherwise.<br/><br/>An <strong>Anagram</strong> is a word or phrase formed by rearranging the letters of a different word or phrase, typically using all the original letters exactly once.`,
    examples: [
      {
        input: 's = "anagram", t = "nagaram"',
        output: "true",
      },
      {
        input: 's = "rat", t = "car"',
        output: "false",
      },
    ],
    constraints: [
      "1 <= s.length, t.length <= 5 * 10^4",
      "s and t consist of lowercase English letters.",
    ],
    hints: [
      "What if inputs contain unicode characters? How would you adapt your solution?",
      "Can we use a frequency map or an array of length 26 to count character occurrences?",
    ],
    starterCodes: {
      Python: `class Solution:\n    def isAnagram(self, s: str, t: str) -> bool:\n        if len(s) != len(t):\n            return False\n        count = {}\n        for char in s:\n            count[char] = count.get(char, 0) + 1\n        for char in t:\n            if char not in count or count[char] == 0:\n                return False\n            count[char] -= 1\n        return True`,
      "C++": `class Solution {\npublic:\n    bool isAnagram(string s, string t) {\n        if (s.length() != t.length()) return false;\n        int freq[26] = {0};\n        for (char c : s) freq[c - 'a']++;\n        for (char c : t) {\n            if (--freq[c - 'a'] < 0) return false;\n        }\n        return true;\n    }\n};`,
      Java: `class Solution {\n    public boolean isAnagram(String s, String t) {\n        if (s.length() != t.length()) return false;\n        int[] count = new int[26];\n        for (char c : s.toCharArray()) count[c - 'a']++;\n        for (char c : t.toCharArray()) {\n            if (--count[c - 'a'] < 0) return false;\n        }\n        return true;\n    }\n}`,
      C: `bool isAnagram(char* s, char* t) {\n    if (strlen(s) != strlen(t)) return false;\n    int counts[26] = {0};\n    for (int i = 0; s[i]; i++) counts[s[i] - 'a']++;\n    for (int i = 0; t[i]; i++) {\n        if (--counts[t[i] - 'a'] < 0) return false;\n    }\n    return true;\n}`,
    },
    testCases: [
      {
        id: "tc-1",
        input: 's = "anagram", t = "nagaram"',
        expectedOutput: "true",
        actualOutput: "true",
        passed: true,
        runtimeMs: 8,
        memoryMb: 14.5,
      },
      {
        id: "tc-2",
        input: 's = "rat", t = "car"',
        expectedOutput: "false",
        actualOutput: "false",
        passed: true,
        runtimeMs: 7,
        memoryMb: 14.4,
      },
    ],
    solved: true,
  },
];
